
#import Builtin.*;

#import Builtin.List;

#import Dev.Builtin.Versioning;

CSV :: struct{
    columns : list<string> = list<string>();
    rowData : list<string> = list<string>();

    load :: bool(path : const string){
        f : File = File(path, "r");

        if(f.isOpen){
            printl(f"File path: {f.path}");
        }
    }
}

main :: (){
    c : CSV = CSV("sample.csv");
}


