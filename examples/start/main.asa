
#use Builtin.*;

local :: module{
    DEF :: 182;
}

i32 :: int32;
someAlias :: "aliased string";

ABC :: 40;

TEST_MACRO_1 :: {printl("Macro works!");};

TEST_MACRO_2 :: (){ printl("Macro 2 works!") };

RENAMED_MACRO :: > TEST_MACRO_1; 

#use Rendering.Raylib;

main :: (){

    x :: 6;

    y : int = default;
    z : int = ?;

    someStr : string = default;

    z = 5;

    printl("y: " + y);
    printl("z: " + z);
    printl("someStr: \"" + someStr + "\"");

    #if(#getflag(COMPILERDEBUG) == false, {
        #error("This program must be run in compiler debug mode (-fcompilerdebug)", #context);
    });


    //#stack_push("a", { printl("hi!"); });

    //#warning("This is a warning", #stack_last("a"));

    //#warning("This is a warning", x);
    //#warning("This is a warning of the definition", #parent(x));

    //printl("This should print first");

    //#print_ast(#parent(0));

    //#print_ast(local);

    #if(x == 5, {
        #printl("it was true!");
    });
    #else_if(true, {
        #printl("else worked");
    });

    printl(local.DEF);
    printl(#nameof(#parent()));

    //#stack_last("a");
}




